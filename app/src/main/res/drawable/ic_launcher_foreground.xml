<?xml version="1.0" encoding="utf-8"?>
<!-- The circular HRIS mark, inset into the adaptive-icon safe zone so it sits centered on the
     background tile (matches the splash logo). -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/hris_logo"
    android:inset="18%" />
