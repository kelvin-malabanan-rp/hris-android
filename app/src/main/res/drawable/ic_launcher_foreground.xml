<?xml version="1.0" encoding="utf-8"?>
<!-- The white RP logo trace (same artwork as the iOS Icon Composer icon), inset into the
     adaptive-icon safe zone so it sits centered on the brand-blue background tile. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/rp_logo_trace"
    android:inset="10%" />
